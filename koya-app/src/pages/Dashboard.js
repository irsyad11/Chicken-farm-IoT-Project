import { useState } from "react";
import { useMediaQuery } from "react-responsive";
import Sidebar from "../layouts/landing/dashboard/Sidebar";

export default function Dashboard() {
  const [Closed, SetClosed] = useState(false);
  return <></>;
}
